if (this.discountRate) base = base - this.discountRate * base;
//=========================================>
assert(this.discountRate >= 0);
if (this.discountRate) base = base - this.discountRate * base;
